node_modules/
dist/
*.sqlite
*.sqlite-wal
*.sqlite-shm
.env
.DS_Store

# Playwright MCP artifacts and captured API payloads (contain PII — never commit)
.playwright-mcp/
ntfy-*.json

# Local git worktrees (Step 1b fallback workspace)
.worktrees/
